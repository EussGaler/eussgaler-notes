# 三、离散信号的分析

## 1. 信号的采样与恢复

### （1）时域采样

记采样周期为 $T_s$，则采样角频率 $\omega_s=2\pi/T_s$。

$$
{\color{#c62828}f_s(t)=f(t)p(t)},\qquad
F(\omega)=\mathcal F[f(t)],\qquad
F_s(\omega)=\mathcal F[f_s(t)].
$$

周期采样函数 $p(t)$ 的频谱为

$$
P(\omega)=\mathcal F[p(t)]
={\color{#c62828}\sum_{n=-\infty}^{+\infty}2\pi P_n\delta(\omega-n\omega_s)}.
$$

由频域卷积定理，

$$
{\color{#c62828}
F_s(\omega)=\frac1{2\pi}F(\omega)*P(\omega)
=\sum_{n=-\infty}^{+\infty}P_nF(\omega-n\omega_s)
}.
$$

这里用到 $F(\omega)*\delta(\omega-n\omega_s)=F(\omega-n\omega_s)$，$P_n$ 为傅里叶系数：

$$
P_n=\frac1{T_s}\int_{-T_s/2}^{T_s/2}p(t)e^{-jn\omega_st}\,\mathrm dt.
$$

### （2）矩形脉冲采样与冲激采样

若为矩形脉冲采样，则

$$
P_n=\frac{E\tau}{T_s}\operatorname{Sa}\left(\frac{n\omega_s\tau}{2}\right),
$$

$$
F_s(\omega)=\frac{E\tau}{T_s}\sum_{n=-\infty}^{+\infty}
\operatorname{Sa}\left(\frac{n\omega_s\tau}{2}\right)F(\omega-n\omega_s).
$$

$F(\omega)$ 按 $\omega_s$ 间隔平移复制，各副本幅度按取样函数系数加权；一般不再严格等幅周期重复。

若为冲激（理想）采样，则

$$
{\color{#c62828}P_n=\frac1{T_s},\qquad
F_s(\omega)=\frac1{T_s}\sum_{n=-\infty}^{+\infty}F(\omega-n\omega_s)}.
$$

表明频谱发生<span style="color:#c62828"><strong>周期延拓</strong></span>，拓展至以 $\pm\omega_s,\pm2\omega_s,\ldots$ 为中心的频谱，频谱幅度乘上 $1/T_s$ 因子。

### （3）<span style="color:#c62828">时域采样定理</span>

若 $x(t)$ 的频谱仅在有限区间 $[-\omega_m,\omega_m]$ 内可能非零，则称为<span style="color:#c62828"><strong>频谱受限的信号</strong></span>。

香农定理：对于频谱受限信号，若最高频率分量为 $\omega_m$（$f_m$），为了保留原信号全部信息并能无失真复原，通常要求

$$
{\color{#c62828}\omega_s>2\omega_m\qquad(f_s>2f_m)}.
$$

边界谱线不存在、满足相应端点条件时可用等号。$2\omega_m$ 称为奈奎斯特采样角频率，$2f_m$ 为奈奎斯特采样率。

工程上一般取 $\omega_s\geq10\omega_m$。

> **【图片占位 3-01】** 采样频谱副本示意：当 $\omega_s<2\omega_m$ 时相邻频谱混叠；保留 $\omega_m,\omega_s$ 与重叠区域。

### （4）频域采样

记周期冲激序列

$$
\delta_T(t)=\sum_{k=-\infty}^{+\infty}\delta(t-kT_0),
\qquad
\mathcal F[\delta_T(t)]
=\omega_0\sum_{k=-\infty}^{+\infty}\delta(\omega-k\omega_0),
\quad\omega_0=\frac{2\pi}{T_0}.
$$

对 $F(\omega)$ 在频域上以 $\omega_0$ 为间隔采样，得到

$$
{\color{#c62828}F_p(\omega)=F(\omega)\delta_\omega(\omega)},
$$

$$
{\color{#c62828}
\delta_\omega(\omega)=\sum_{k=-\infty}^{+\infty}\delta(\omega-k\omega_0)
}=\frac1{\omega_0}\mathcal F[\delta_T(t)].
$$

由时域卷积定理，

$$
\begin{aligned}
{\color{#c62828}f_p(t)}
&={\color{#c62828}\mathcal F^{-1}[F_p(\omega)]
=f(t)*\mathcal F^{-1}[\delta_\omega(\omega)]}\\
&=f(t)*\frac1{\omega_0}\delta_T(t)
={\color{#c62828}\frac1{\omega_0}\sum_{k=-\infty}^{+\infty}f(t-kT_0)}.
\end{aligned}
$$

表明对应时域信号以 <span style="color:#c62828"><strong>$T_0$ 为周期</strong></span>对原信号延拓，幅度乘以 $1/\omega_0$。

频域采样定理：若 $f(t)$ 为<span style="color:#c62828"><strong>时间受限信号</strong></span>，集中在 $[-t_m,t_m]$ 内，则为避免时域副本重叠，需 $T_0\geq2t_m$。相应的采样间隔为

$$
{\color{#c62828}\Delta f\leq\frac1{2t_m}}
\quad\text{或}\quad
\Delta\omega\leq\frac\pi{t_m}.
$$

采样后的频谱可唯一表示原信号；端点按不重叠的相应条件处理。这里须区别普通频率与角频率。

### （5）从时域采样信号恢复原信号

由于频域延拓，需取<span style="color:#c62828"><strong>主频带（加窗）</strong></span>：

$$
{\color{#c62828}F(\omega)=F_s(\omega)H(\omega)}.
$$

取理想重建滤波器

$$
H(\omega)=\begin{cases}T_s,&|\omega|<\omega_c,\\0,&|\omega|>\omega_c,\end{cases}
\qquad \omega_c=\frac{\omega_s}{2}=\frac\pi{T_s}.
$$

$$
h(t)=\mathcal F^{-1}[H(\omega)]
=\operatorname{Sa}(\omega_ct).
$$

于是

$$
f(t)=f_s(t)*h(t)
=\sum_{n=-\infty}^{+\infty}f(nT_s)
\operatorname{Sa}\left(\frac{\omega_s}{2}(t-nT_s)\right).
$$

这就是时域内插公式。只要 $f(t)$ 带限且满足采样定理，即可实现信号重建。

对非带限信号，采样前先用抗混叠滤波器滤除高频成分；恢复的是滤波后的信号。

### （6）从频域采样信号恢复原信号

需在时域加窗：

$$
{\color{#c62828}f(t)=f_p(t)g(t)},
$$

$$
g(t)=\begin{cases}\omega_0,&|t|<T_0/2,\\0,&|t|>T_0/2.\end{cases}
$$

故

$$
F(\omega)=\frac1{2\pi}F_p(\omega)*G(\omega)
=\sum_{k=-\infty}^{+\infty}F(k\omega_0)
\operatorname{Sa}\left(\frac{T_0}{2}(\omega-k\omega_0)\right).
$$

这是频域内插公式。

## 2. 离散信号的时域描述和运算

### （1）几种典型离散信号（序列）

#### 单位脉冲序列（单位样值信号）

$$
\delta(n)=\begin{cases}1,&n=0,\\0,&n\neq0,\end{cases}
\qquad \sum_{n=-\infty}^{+\infty}\delta(n)=1.
$$

也具有取样特性。

#### 单位阶跃序列

$$
u(n)=\begin{cases}1,&n\geq0,\\0,&n<0,\end{cases}
$$

$$
\delta(n)=u(n)-u(n-1),\qquad
u(n)=\sum_{k=0}^{\infty}\delta(n-k).
$$

#### 矩形序列

$$
R_N(n)=\begin{cases}1,&0\leq n\leq N-1,\\0,&\text{其他},\end{cases}
$$

$$
R_N(n)=u(n)-u(n-N)
=\delta(n)+\delta(n-1)+\cdots+\delta(n-(N-1)).
$$

#### 斜变序列

$$
R(n)=nu(n).
$$

抛物线序列可记为 $r(n)=n^2u(n)$。

#### 实指数序列（单边指数序列）

$$
x(n)=a^nu(n).
$$

#### 正弦型序列

$$
x(n)=A\sin(n\Omega_0+\varphi).
$$

其中 $\Omega_0$ 为数字角频率，$\varphi$ 为初始相角。

若由 $x(t)=A\sin\omega_0t$ 采样得到，$t=nT_s$，则

$$
x(n)=x(nT_s)=A\sin(n\omega_0T_s),
\qquad
\boxed{\color{#c62828}\Omega_0=\omega_0T_s=\frac{\omega_0}{f_s}}.
$$

与连续正弦信号的区别：

1. 连续：时移与相移一一对应。离散：整数时移只产生 $\Omega_0$ 的整数倍相移（模 $2\pi$），并非任意相移均能由整数时移实现。
2. 离散正弦不一定为周期序列；非退化情形要求 $\Omega_0/(2\pi)$ 为有理数。
3. 两个相同离散正弦序列的数字角频率<span style="color:#c62828"><strong>可能不同</strong></span>，例如相差 $2\pi$。

#### 复指数序列

$$
x(n)=e^{(\sigma+j\Omega_0)n}
=e^{\sigma n}\bigl(\cos\Omega_0n+j\sin\Omega_0n\bigr).
$$

与正弦序列类似，作为 $\Omega_0$ 的函数以 $2\pi$ 为周期，故数字角频率有效范围可选 <span style="color:#c62828"><strong>$[0,2\pi)$ 或 $[-\pi,\pi)$</strong></span>。

#### 任意离散序列

$$
x(n)=\sum_{m=-\infty}^{+\infty}x(m)\delta(n-m),
$$

即表示为加权、平移的脉冲信号之和。

### （2）离散信号的时域运算

① 平移：向右 $x(n-m)$，向左 $x(n+m)$；翻转：$x(-n)$。

② <span style="color:#c62828"><strong>和：指同序号</strong></span>的序列值逐项相加：

$$
z(n)=x(n)+y(n).
$$

<span style="color:#c62828"><strong>积：同序号</strong></span>序列值逐项相乘：

$$
z(n)=x(n)y(n).
$$

③ 累加：$x(n)$ 的累加序列为

$$
{\color{#c62828}y(n)=\sum_{k=-\infty}^{n}x(k)}.
$$

④ 差分运算：

$$
\text{前向差分：}\quad{\color{#c62828}\Delta x(n)=x(n+1)-x(n)},
$$

$$
\text{后向差分：}\quad{\color{#c62828}\nabla x(n)=x(n)-x(n-1)=\Delta x(n-1)}.
$$

⑤ 时间尺度变换：

$$
{\color{#c62828}x(n)\to x(mn),\ x(n/m)},\qquad m\in\mathbb Z_+.
$$

$x(mn)$ 表示抽取，采样率降低为原来的 $1/m$。$x(n/m)$ 用于表示扩展序列时，须规定非整数索引处插零：

$$
x_{\uparrow m}(n)=
\begin{cases}x(n/m),&n\text{ 为 }m\text{ 的整数倍},\\0,&\text{其他}.\end{cases}
$$

这称为插零扩展；若要形成真正的<span style="color:#c62828"><strong>插值序列</strong></span>，还需相应插值滤波。

### （3）线性系统的单位脉冲响应

#### ① 一般线性系统

用 <span style="color:#c62828"><strong>$h_m(n)$</strong></span> 表示离散时间线性系统对移位单位脉冲 <span style="color:#c62828"><strong>$\delta(n-m)$</strong></span> 的响应。

> **【图片占位 3-02】** 线性系统方框：一般输入 $x(n)$ 对应输出 $y(n)$；移位脉冲 $\delta(n-m)$ 对应 $h_m(n)$。

由叠加性，

$$
x(n)=\sum_{m=-\infty}^{+\infty}x(m)\delta(n-m)
\quad\Rightarrow\quad
y(n)=\sum_{m=-\infty}^{+\infty}x(m)h_m(n).
$$

#### ② <span style="color:#c62828">线性时不变（LTI）系统</span>

有 $h_m(n)=h_0(n-m)$，即 $\delta(n)$ 移位，响应也随之移位。

令 <span style="color:#c62828"><strong>$h(n)=h_0(n)$</strong></span>，则

$$
y(n)=\sum_{m=-\infty}^{+\infty}x(m)h(n-m)
={\color{#c62828}x(n)*h(n)}.
$$

LTI 系统的单位脉冲响应完全刻画其零状态输入输出特征。

#### ③ <span style="color:#c62828">卷积和</span>

对两序列 $x(n),y(n)$，定义

$$
z(n)=\sum_{m=-\infty}^{+\infty}x(m)y(n-m)=x(n)*y(n).
$$

计算步骤：

- 换坐标：将 $m$ 作为自变量，$n$ 作为参变量。
- 翻转：将 $y(m)$ 变为 $y(-m)$。
- 平移：计算 $z(n)$ 时，将 $y(-m)$ 向右平移 $n$，变为 $y(n-m)$。
- 最终相乘、累加。

<span style="color:#c62828"><strong>长度为 $N,M$ 的有限序列，线性卷积长度至多为 $N+M-1$；端点非零时即为 $N+M-1$。</strong></span>

性质：

$$
x*h=h*x,\qquad
x*(h_1+h_2)=x*h_1+x*h_2,
$$

$$
x*(h_1*h_2)=(x*h_1)*h_2.
$$

与单位脉冲的卷积：

$$
{\color{#c62828}x(n-m)*\delta(n-k)=x(n-m-k)}.
$$

差分：

$$
\Delta(x*y)=x*(\Delta y)=(\Delta x)*y.
$$

累加：

$$
\sum_{k=-\infty}^{n}(x*y)(k)
=x(n)*\left(\sum_{k=-\infty}^{n}y(k)\right)
=\left(\sum_{k=-\infty}^{n}x(k)\right)*y(n).
$$

## 3. 离散信号的频域分析

### （1）周期信号的频域分析

#### ① 推导<span style="color:#c62828">离散傅里叶级数（DFS）</span>

考虑连续周期信号的 Fourier 级数：

$$
x(t)=\sum_{k=-\infty}^{+\infty}X_c(k\omega_0)e^{jk\omega_0t},
\qquad
X_c(k\omega_0)=\frac1{T_0}\int_0^{T_0}x(t)e^{-jk\omega_0t}\,\mathrm dt.
$$

注意，此处 $x(t)$ 为连续信号，周期 $T_0=2\pi/\omega_0$。

对一个周期进行 <span style="color:#c62828"><strong>$N$ 点采样</strong></span>：

$$
T_0=NT_s,\qquad \omega_0=\frac{2\pi}{NT_s},
\qquad \Omega_0=\omega_0T_s=\frac{2\pi}{N}.
$$

$k\Omega_0$ 为 $k$ 次谐波数字角频率；独立频率分量只有 $N$ 个，取 $k=0,\ldots,N-1$。

将连续积分以一个周期内的取样和类比：

$$
\frac1{NT_s}\sum_{n=0}^{N-1}x(nT_s)
e^{-jk(2\pi/(NT_s))nT_s}T_s
={\color{#c62828}\frac1N\sum_{n=0}^{N-1}x(nT_s)e^{-jk\Omega_0n}}.
$$

该式定义采样序列的 DFS 系数；一般不能把有限求和不加条件地当成原连续积分的精确值。

<span style="color:#c62828"><strong>将 $x(nT_s)$ 记为 $x(n)$，离散系数记为 $X(k\Omega_0)$：</strong></span>

$$
{\color{#c62828}
X_k=X(k\Omega_0)=\frac1N\sum_{n=0}^{N-1}x(n)e^{-jk\Omega_0n}
},\qquad k=0,1,\ldots,N-1.
$$

由于 $X(k\Omega_0)$ 对下标 $k$ 以 $N$ 为周期，离散周期信号只有<span style="color:#c62828"><strong>有限个独立谐波分量</strong></span>，谐波数 $2\pi/\Omega_0=N$，故

$$
{\color{#c62828}
x(n)=\sum_{k=0}^{N-1}X(k\Omega_0)e^{jk\Omega_0n}
},\qquad n=0,1,\ldots,N-1,
$$

并按周期延拓到所有整数 $n$。

#### ② <span style="color:#c62828">离散 Fourier 级数变换对</span>

$$
{\color{#c62828}x(n)\xleftrightarrow{\mathrm{DFS}}X(k\Omega_0)}.
$$

正变换：

$$
X(k\Omega_0)=\frac1N\sum_{n\in\langle N\rangle}x(n)e^{-jk\Omega_0n}.
$$

反变换：

$$
x(n)=\sum_{k\in\langle N\rangle}X(k\Omega_0)e^{jk\Omega_0n}.
$$

$\langle N\rangle$ 表示任取连续的 $N$ 个整数。离散周期序列对应离散且周期的频谱：时域周期化，对应频域离散化。

<span style="color:#c62828"><strong>因此只有离散周期序列才能直接进行 DFS；有限序列可先作周期延拓。</strong></span>

#### ③ 连续和离散周期信号频谱的关系

连续 Fourier 级数：

$$
x(t)\xleftrightarrow{\mathrm{CFS}}X_c(k\omega_0),
\qquad T_0,\quad \omega_0=\frac{2\pi}{T_0}.
$$

离散 Fourier 级数：

$$
x(n)\xleftrightarrow{\mathrm{DFS}}X(k\Omega_0),
\qquad N=\frac{T_0}{T_s},\quad {\color{#c62828}\Omega_0=\frac{2\pi}{N}}.
$$

<span style="color:#c62828"><strong>满足采样定理</strong></span>且无谐波混叠时，DFS 系数是连续级数系数在下标上以 $N$ 为周期的复制；一般关系为

$$
X(k\Omega_0)=\sum_{\ell=-\infty}^{+\infty}X_c((k+\ell N)\omega_0).
$$

频率坐标满足 $\Omega=\omega T_s$，延拓周期为 $2\pi$（数字角频率）或 $\omega_s=2\pi/T_s$（模拟角频率）。归一化 DFS 系数不额外乘 $1/T_s$；该因子属于连续冲激采样信号的 Fourier 变换。

$X(k\Omega_0)$ 为周期的离散系数序列，$X_c(k\omega_0)$ 为一般不周期的离散系数序列；无混叠时一段主值与原系数对应。

例：$x(t)=6\cos\pi t$，以 $T_s=0.25\,\mathrm s$ 采样：

$$
\Omega_0=\frac\pi4,\qquad N=\frac{2\pi}{\Omega_0}=8.
$$

$$
|X(k\Omega_0)|=
\begin{cases}3,&k=8\ell\pm1,\ \ell\in\mathbb Z,\\0,&\text{其他},\end{cases}
$$

而原信号幅度谱为

$$
|X_c(k\omega_0)|=
\begin{cases}3,&k=\pm1,\\0,&\text{其他}.\end{cases}
$$

> **【图片占位 3-03】** 左页例题四幅谱图：以 $k$ 为横轴的 DFS 周期谱（$k=\pm1,\pm7,\pm9,\ldots$）和连续级数谱（仅 $k=\pm1$）；以角频率为横轴时，数字频率标 $\pm\pi/4$ 及 $2\pi$ 周期，模拟频率标 $\pm\pi$。谱线高度均为 $3$。

#### ④ DFS 的主要性质

线性：

$$
ax(n)+by(n)\xleftrightarrow{\mathrm{DFS}}
aX(k\Omega_0)+bY(k\Omega_0).
$$

周期卷积定理：定义两<span style="color:#c62828"><strong>周期序列</strong></span>的周期卷积

$$
x(n)\circledast h(n)
=\sum_{m=0}^{N-1}x(m)h(n-m)=h(n)\circledast x(n).
$$

则

$$
{\color{#c62828}
x\circledast h\xleftrightarrow{\mathrm{DFS}}NX(k\Omega_0)H(k\Omega_0)
},
$$

$$
{\color{#c62828}
x(n)h(n)\xleftrightarrow{\mathrm{DFS}}
X(k\Omega_0)\circledast H(k\Omega_0)
}.
$$

复共轭：

$$
{\color{#c62828}x^*(-n)\xleftrightarrow{\mathrm{DFS}}X^*(k\Omega_0)}.
$$

位移性质：

$$
{\color{#c62828}
x(n-m)\xleftrightarrow{\mathrm{DFS}}e^{-jk\Omega_0m}X(k\Omega_0)
}.
$$

帕斯瓦尔定理：

$$
{\color{#c62828}
\frac1N\sum_{n=0}^{N-1}x(n)h^*(n)
=\sum_{k=0}^{N-1}X(k\Omega_0)H^*(k\Omega_0)
}.
$$

特别地，$x=h$ 时

$$
{\color{#c62828}
\frac1N\sum_{n=0}^{N-1}|x(n)|^2
=\sum_{k=0}^{N-1}|X(k\Omega_0)|^2
}.
$$

### （2）从 DFS 到<span style="color:#c62828">离散时间傅里叶变换（DTFT）</span>

#### ① 推导

将长度有限的非周期信号 $x(n)$ 以 $N$ 为周期延拓为周期信号 $x_N(n)$：

$$
X_N(k\Omega_0)=\frac1N\sum_{n\in\langle N\rangle}x(n)e^{-jk\Omega_0n}
=\frac1N\sum_{n\in\langle N\rangle}x(n)e^{-jk(2\pi/N)n}.
$$

当 $N\to\infty$，$x_N(n)\to x(n)$，但单个归一化系数趋于 $0$。<span style="color:#c62828"><strong>定义频谱密度函数：</strong></span>

$$
{\color{#c62828}
X(\Omega)=\lim_{\substack{N\to\infty\\k\Omega_0\to\Omega}}
NX_N(k\Omega_0)
=\sum_{n=-\infty}^{+\infty}x(n)e^{-j\Omega n}
}.
$$

此时

$$
\begin{aligned}
x(n)
&=\lim_{N\to\infty}\sum_{k=0}^{N-1}X_N(k\Omega_0)e^{jk\Omega_0n}\\
&=\lim_{N\to\infty}\frac1N\sum_{k=0}^{N-1}
NX_N(k\Omega_0)e^{jk\Omega_0n}\\
&=\frac1{2\pi}\int_0^{2\pi}X(\Omega)e^{j\Omega n}\,\mathrm d\Omega.
\end{aligned}
$$

因为 <span style="color:#c62828"><strong>$N\to\infty$ 时 $\Omega_0=2\pi/N\to\mathrm d\Omega$，$k\Omega_0\to\Omega$。</strong></span>

$X(\Omega)$ 为周期函数：

$$
{\color{#c62828}X(\Omega+2\pi)=X(\Omega)}.
$$

采样得到的频谱副本若重叠，会发生混叠。

#### ② <span style="color:#c62828">离散时间 Fourier 变换对</span>

$$
{\color{#c62828}x(n)\xleftrightarrow{\mathrm{DTFT}}X(\Omega)}.
$$

正变换：序列 $x(n)$ 到频谱密度函数 $X(\Omega)$，也表示为 $X(e^{j\Omega})$：

$$
{\color{#c62828}
X(\Omega)=\sum_{n=-\infty}^{+\infty}x(n)e^{-j\Omega n}
}.
$$

反变换：

$$
{\color{#c62828}
x(n)=\frac1{2\pi}\int_0^{2\pi}X(\Omega)e^{j\Omega n}\,\mathrm d\Omega
=\frac1{2\pi}\int_{-\pi}^{\pi}X(\Omega)e^{j\Omega n}\,\mathrm d\Omega
}.
$$

存在条件：无限长序列若绝对可和，

$$
\sum_{n=-\infty}^{+\infty}|x(n)|<\infty,
$$

则 DTFT 一致收敛且连续；若能量有限，

$$
\sum_{n=-\infty}^{+\infty}|x(n)|^2<\infty,
$$

则可按均方意义定义 DTFT，不能据此保证所有频率点逐点收敛。

左侧补充的变换对：

$$
a^nu(n)\xleftrightarrow{\mathrm{DTFT}}
\frac1{1-ae^{-j\Omega}},\qquad |a|<1,
$$

$$
-a^nu(-n-1)\xleftrightarrow{\mathrm{DTFT}}
\frac1{1-ae^{-j\Omega}},\qquad |a|>1.
$$

可用于求解线性常系数差分方程：两边取 DTFT。

#### ③ DTFT 的性质

线性：

$$
ax(n)+by(n)\xleftrightarrow{\mathrm{DTFT}}aX(\Omega)+bY(\Omega).
$$

时域平移与频域平移：

$$
{\color{#c62828}
x(n-n_0)\xleftrightarrow{\mathrm{DTFT}}e^{-j\Omega n_0}X(\Omega)
},
$$

$$
{\color{#c62828}
e^{j\Omega_0n}x(n)\xleftrightarrow{\mathrm{DTFT}}X(\Omega-\Omega_0)
}.
$$

时域卷积：

$$
x(n)*y(n)\xleftrightarrow{\mathrm{DTFT}}X(\Omega)Y(\Omega).
$$

频域周期卷积：

$$
x(n)y(n)\xleftrightarrow{\mathrm{DTFT}}
\frac1{2\pi}\int_{-\pi}^{\pi}X(\lambda)Y(\Omega-\lambda)\,\mathrm d\lambda.
$$

共轭对称：

$$
x^*(n)\xleftrightarrow{\mathrm{DTFT}}X^*(-\Omega).
$$

频域微分：

$$
nx(n)\xleftrightarrow{\mathrm{DTFT}}j\frac{\mathrm dX(\Omega)}{\mathrm d\Omega}.
$$

#### ④ 与连续时间傅里叶变换（CTFT）的关系

此处比较非周期信号：二者频域均连续，但 CTFT 的 $X(\omega)$ 一般不周期，DTFT 的 $X(\Omega)$ 为 $2\pi$ 周期函数。

### （3）从 DFS 到<span style="color:#c62828">离散傅里叶变换（DFT）</span>

#### ① 从周期延拓出发

将有限长序列 $x(n)$（$0\leq n\leq N-1$，区间外为 $0$）按周期 $N$ 延拓：

$$
x_p(n)=\sum_{r\in\mathbb Z}x(n+rN).
$$

称 $x(n)$ 为主值序列，$x_p(n)$ 的 $0\leq n\leq N-1$ 为主值区间。

由 DFS 与 IDFS：

$$
X_p(k\Omega_0)=\frac1N\sum_{n=0}^{N-1}x_p(n)e^{-jk\Omega_0n},
$$

$$
x_p(n)=\sum_{k=0}^{N-1}X_p(k\Omega_0)e^{jk\Omega_0n}.
$$

由系数周期性，可取 $0\leq k\leq N-1$ 为主值区间，主值内有

$$
X_{\mathrm{DFS}}(k\Omega_0)=\frac1N\sum_{n=0}^{N-1}x(n)e^{-jk\Omega_0n},
\qquad
x(n)=\sum_{k=0}^{N-1}X_{\mathrm{DFS}}(k\Omega_0)e^{jk\Omega_0n}.
$$

<span style="color:#c62828"><strong>定义 $X(k)=NX_{\mathrm{DFS}}(k\Omega_0)$，$\Omega_0=2\pi/N$。</strong></span>

$$
{\color{#c62828}
X(k)=\sum_{n=0}^{N-1}x(n)e^{-j(2\pi/N)kn}
},\qquad k=0,1,\ldots,N-1,
$$

$$
{\color{#c62828}
x(n)=\frac1N\sum_{k=0}^{N-1}X(k)e^{j(2\pi/N)kn}
},\qquad n=0,1,\ldots,N-1.
$$

即 DFT 与 IDFT。与 DTFT 不同，这里频域也离散化。

#### ② 从 DTFT 到 DFT

在频域一个主周期内，按采样间隔 $\Omega_0=2\pi/N$ 将连续频域函数 $X(\Omega)$ 离散化：

$$
X(k)=\left.X(\Omega)\right|_{\Omega=2\pi k/N}
=\operatorname{DFT}[x(n)].
$$

由频域采样定理，对应时域序列会以 $N$ 为周期延拓，故要避免时域混叠，原有限序列长度 $L$ 应满足 $L\leq N$，通常取 $L=N$。

### （4）<span style="color:#c62828">DFT 的性质</span>

#### ① 线性

$$
ax_1(n)+bx_2(n)\xleftrightarrow{\mathrm{DFT}}aX_1(k)+bX_2(k).
$$

#### ② <span style="color:#c62828">圆周位移性质</span>

$$
{\color{#c62828}
x((n-m)_N)R_N(n)\xleftrightarrow{\mathrm{DFT}}
X(k)e^{-jk\Omega_0m}
}.
$$

其中 $(n-m)_N$ 表示模 $N$ 后在 $0,\ldots,N-1$ 的代表元，$R_N(n)$ 为长度 $N$ 的矩形序列。

同理，

$$
{\color{#c62828}
x(n)e^{jk_0\Omega_0n}\xleftrightarrow{\mathrm{DFT}}
X((k-k_0)_N)R_N(k)
}.
$$

#### ③ <span style="color:#c62828">圆周卷积性质</span>

<span style="color:#c62828"><strong>$x(n),h(n)$ 均为长度 $N$ 的有限长序列。</strong></span>

时域圆周卷积：

$$
{\color{#c62828}
x(n)\circledast_Nh(n)\xleftrightarrow{\mathrm{DFT}}X(k)H(k)
}.
$$

定义：

$$
\begin{aligned}
x(n)\circledast_Nh(n)
&=\left[\sum_{m=0}^{N-1}x(m)h((n-m)_N)\right]R_N(n)\\
&=\left[\sum_{m=0}^{N-1}h(m)x((n-m)_N)\right]R_N(n).
\end{aligned}
$$

频域圆周卷积：

$$
{\color{#c62828}
x(n)h(n)\xleftrightarrow{\mathrm{DFT}}
\frac1N X(k)\circledast_NH(k)
}.
$$

与线性卷积的关系：设有限长序列 $x(n),h(n)$ 长度分别为 $N,M$，记 $y(n)=x(n)*h(n)$，则长度至多为 $N+M-1$。将两序列补零至长度 $L$，当

$$
{\color{#c62828}L\geq N+M-1}
$$

时，在 $0\leq n\leq L-1$ 内有

$$
{\color{#c62828}x(n)\circledast_Lh(n)=x(n)*h(n)}.
$$

#### ④ 奇偶虚实性

按模 $N$ 的圆周奇偶性理解：

- 实偶序列 $\to$ 实偶序列。
- 实奇序列 $\to$ 虚奇序列。
- 虚偶序列 $\to$ 虚偶序列。
- 虚奇序列 $\to$ 实奇序列。

## 4. 快速傅里叶变换及应用

### （1）DFT 的矩阵形式

$$
X(k)=\sum_{n=0}^{N-1}x(n)e^{-j(2\pi/N)nk},
\qquad k=0,\ldots,N-1.
$$

记 <span style="color:#c62828"><strong>$W_N=e^{-j2\pi/N}$ 为旋转因子</strong></span>，则

$$
{\color{#c62828}X(k)=\sum_{n=0}^{N-1}x(n)W_N^{nk}},
\qquad k=0,\ldots,N-1.
$$

矩阵形式：

$$
\begin{bmatrix}X(0)\\X(1)\\\vdots\\X(N-1)\end{bmatrix}
=
\begin{bmatrix}
W_N^0&W_N^0&\cdots&W_N^0\\
W_N^0&W_N^1&\cdots&W_N^{N-1}\\
\vdots&\vdots&&\vdots\\
W_N^0&W_N^{N-1}&\cdots&W_N^{(N-1)^2}
\end{bmatrix}
\begin{bmatrix}x(0)\\x(1)\\\vdots\\x(N-1)\end{bmatrix}.
$$

### （2）基 2 FFT 算法

取 $N$ 为 $2$ 的幂，奇偶分离：

$$
\begin{aligned}
X(k)
&=\sum_{\ell=0}^{N/2-1}x(2\ell)W_N^{2\ell k}
+\sum_{\ell=0}^{N/2-1}x(2\ell+1)W_N^{(2\ell+1)k}\\
&=\sum_{\ell=0}^{N/2-1}x(2\ell)W_{N/2}^{\ell k}
+W_N^k\sum_{\ell=0}^{N/2-1}x(2\ell+1)W_{N/2}^{\ell k}\\
&\triangleq{\color{#c62828}G(k)+W_N^kH(k)}.
\end{aligned}
$$

分解为两个 $N/2$ 点 DFT。由对称性，

$$
{\color{#c62828}
X(k)=G(k)+W_N^kH(k)
},\qquad k=0,1,\ldots,N/2-1,
$$

$$
{\color{#c62828}
X(k+N/2)=G(k)-W_N^kH(k)
},\qquad k=0,1,\ldots,N/2-1.
$$

将 $G(k),H(k)$ 进一步分解，最终分解为 $2$ 点 DFT。

### （3）蝶形运算：以 $N=8$ 为例

将 $x(n)$ 码位倒置：

| 原下标 | 二进制 | 倒置 | 新下标 |
| --- | --- | --- | --- |
| 0 | 000 | 000 | 0 |
| 1 | 001 | 100 | 4 |
| 2 | 010 | 010 | 2 |
| 3 | 011 | 110 | 6 |
| 4 | 100 | 001 | 1 |
| 5 | 101 | 101 | 5 |
| 6 | 110 | 011 | 3 |
| 7 | 111 | 111 | 7 |

基 2 FFT 流程依次为 <span style="color:#c62828"><strong>2 点、4 点、8 点 DFT</strong></span>。

> **【图片占位 3-04】** 完整 8 点基 2 按时间抽取 FFT 流图：输入顺序 $x(0),x(4),x(2),x(6),x(1),x(5),x(3),x(7)$，输出自然顺序 $X(0)$ 至 $X(7)$；保留三级蝶形、全部旋转因子与 $-1$ 支路。后续须逐条核验连接，不以近似结构替代。

其中

$$
W_4^0=W_8^0,\qquad W_4^1=W_8^2,\qquad W_2^0=W_8^0.
$$

因此该例只需计算 $W_8^0,W_8^1,W_8^2,W_8^3$。

### （4）FFT 的应用

求线性卷积、做连续时间信号的频谱分析等。

## 5. 离散信号的 $z$ 域分析

### （1）<span style="color:#c62828">从 DTFT 到 $z$ 变换</span>

增长型右边离散信号 $x(n)$ 的 Fourier 变换可能不收敛，需乘实指数 $r^{-n}$，选适当 $r>1$ 使加权序列可和：

$$
\operatorname{DTFT}[x(n)r^{-n}]
=\sum_{n=-\infty}^{+\infty}x(n)(re^{j\Omega})^{-n}.
$$

<span style="color:#c62828"><strong>令复变量 $z=re^{j\Omega}$，则 $x(n)$ 的 $z$ 变换为</strong></span>

$$
{\color{#c62828}X(z)=\sum_{n=-\infty}^{+\infty}x(n)z^{-n}}.
$$

<span style="color:#c62828"><strong>$z$ 反变换：</strong></span>

$$
{\color{#c62828}
x(n)=\frac1{2\pi j}\oint_C X(z)z^{n-1}\,\mathrm dz
}.
$$

$C$ 为以原点为圆心、半径 $r$ 且位于收敛域内的逆时针圆周。

### （2）$z$ 变换的收敛域

比值判别法：

$$
\lim_{n\to\infty}\left|\frac{a_{n+1}}{a_n}\right|=\rho.
$$

根值判别法：

$$
\limsup_{n\to\infty}\sqrt[n]{|a_n|}=\rho.
$$

$\rho>1$：级数发散；$\rho<1$：级数绝对收敛；$\rho=1$：不能据此判断，可能收敛也可能发散。

$z$ 变换的几何表示：在 $z$ 平面内标出 $X(z)$ 的零、极点，并标出圆环收敛域。

### （3）单边 $z$ 变换

可看作 $x(n)u(n)$ 的双边 $z$ 变换。本课程应用中常不强调收敛域，但其存在性仍需相应收敛条件。

### （4）与 Laplace 变换的关系

对冲激采样信号

$$
x_s(t)=x(t)\delta_T(t)
=\sum_nx(nT)\delta(t-nT),
\qquad X_s(s)=\mathcal L_b[x_s(t)],
$$

有

$$
\left.X_s(s)\right|_{e^{sT}=z}
=X(z)=\mathcal Z[x(n)].
$$

即冲激采样后进行 $z=e^{sT}$ 映射。

<span style="color:#c62828"><strong>从 $s$ 平面到 $z$ 平面的映射：</strong></span>

$$
s=\sigma+j\omega
\quad\Rightarrow\quad
z=e^{\sigma T}e^{j\omega T},
\qquad |z|=e^{\sigma T}.
$$

该映射是多对一，而不是一一映射；反求 $s$ 时对数有多个分支。

与 DTFT 的关系：令 $r=1$，则 $r^{-n}=1$。若单位圆 $|z|=1$ 在收敛域内，

$$
\left.X(z)\right|_{z=e^{j\Omega}}
=\operatorname{DTFT}[x(n)]=X(\Omega).
$$

与 DFT 的关系：对长度不超过 $N$ 的有限序列，DFT 是 $z$ 变换在单位圆上的 $N$ 点等间隔采样。
